{% extends 'base.html.twig' %}

{% block content %}

    <h3>Inscription</h3>

    {% if notification %}
        <div class="alert alert-info">{{ notification }}</div>
        <hr>
    {% endif %}

    {{ form(form) }}

{% endblock %}